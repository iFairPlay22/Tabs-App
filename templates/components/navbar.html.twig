<nav class="navbar fixed-top navbar-expand-lg navbar-expand-sm navbar-dark bg-primary">
	<a class="navbar-brand light-color" href="{{ path('app_home') }}">Tabs@app</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<ul class="navbar-collapse py-2 collapse navbar-nav" id="navbarNavDropdown">
		{% if is_granted('IS_AUTHENTICATED_FULLY') %}
			<a class="nav-link" href="{{ path('bands_all') }}">
				Bands
			</a>
			{% if band is defined and band.id%}
				<a class="nav-link" href="{{ path('bands_one', { band : band.id }) }}">
					Songs
				</a>
			{% endif %}
			<a class="nav-link" href="{{ path('app_account_edit') }}">
				Account
			</a>
			<a class="nav-link" href="{{ path('app_logout') }}">
				Logout
			</a>
		{% else %}
			<a class="nav-link" href="{{ path('app_sign_up') }}">
				Sign Up
			</a>
			<a class="nav-link" href="{{ path('app_login') }}">
				Login
			</a>
		{% endif %}
	</ul>
</nav>
