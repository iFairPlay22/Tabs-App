{% extends 'base.html.twig' %}

{% block body %}

<div class="container-fluid h-100">
    <div class="h-100 row align-items-center justify-content-center">
        <div class="col-lg-4 col-md-8 col-sm-10 col-12">
            <div class="card">
                <div class="card-body rounded border border-primary">
                    <h1 class="card-title">Tabs@app</h1>
                        <p class="card-text">
                            Gérez vos groupes de musiques, vos morceaux et vos tablatures.
                            Une application gratuite destinée à tous les musiciens.
                        </p>
                    <div class="d-flex justify-content-end align-items-center">
                        <a href="{{ path('app_sign_up') }}" class="btn btn-primary mr-3">
                            Sign Up
                        </a>
                        <a href="{{ path('app_login') }}" class="btn btn-primary">
                            Login
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div> 
</div>

{% endblock %}